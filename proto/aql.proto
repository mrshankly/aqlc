syntax = "proto3";

message Request {
    enum Type {
        QUERY = 0;
        METADATA = 1;
    }

    Type type = 1;
    bytes query = 2;
}

message QueryResponse {
    bytes response = 1;
    bytes error = 2;
}
